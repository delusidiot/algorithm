# Reculsion
- 적어도 하나의 base case, 즉 순환되지 않고 종료되는 case가 있어야 합니다.
- 모든 case는 결국 base case로 수렴해야 합니다.

## Reculsion으로 변경시
- 암시적(implicit) 매개변수를 명시적(explicit) 매개변수로 바꾸어라
순차 검색에서 0에서 시작한다는 것은 암시적인 원칙입니다. 이것을 변수에 지정하여 저장하는 방식으로 다음에 다시 같은 함수를 불러올 때 명시적으로 지정해 줄 수 있게 만든다???